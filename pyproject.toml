[project]
name = "pyheritage"
version = "0.1.0"
description = "Pydantic models for the CIDOC Conceptual Reference Model (CRM)"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "pydantic>=2.12.4",
]

[dependency-groups]
dev = [
    "pytest>=9.0.1",
]
